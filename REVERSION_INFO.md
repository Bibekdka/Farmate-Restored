# Farmate Reverted State

This directory contains a copy of the Farmate project reverted to commit `92e751e2038cb56e8dd0184a506f2a0f41220a28`.

## How to use:
1. A virtual environment has been created in `venv`.
2. Dependencies from `requirements.txt` are being installed.
3. You can run the application using `python app.py` (after activating the venv).

This state represents the project before the major branching/restructuring.
